// 1Наведіть приклади з використанням унарних, бінарних, тернарних операторів (якомога більше різних операторів) js
// Унарні оператори
let x = 5;
let y = -x; // унарний мінус
let z = !true; // логічне заперечення
let a = x++; // інкремент
let b = x--; // декремент
let c = typeof x; // оператор типу

// Бінарні оператори
let sum = 10 + 5; // додавання
let mult = 20 * 4; // множення
let div = 15 / 3; // ділення
let comparison = x >= y; // порівняння
let and = true && false; // логічне І
let or = true || false; // логічне АБО

// Тернарний оператор
let age = 20;
let isAdult = (age >= 18) ? "Дорослий" : "Не дорослий";  // "Дорослий"

let score = 85;
let grade = (score > 90) ? "A" :
            (score > 80) ? "B" :
            (score > 70) ? "C" :
            (score > 60) ? "D" : "F";  // "B"

// Тернарний оператор з логічним виразом
let t = 5;
let result = t > 10 ? "Більше 10" : t > 5 ? "Більше 5, але менше 10" : "5 або менше";

/* 2 Написати скріпт використовуючи метод concat() на 
тему: 
Нехай завжди буде сонце, 
Нехай завжди буде небо, 
Нехай завжди буде мама, 
Нехай завжди буду я. */

// Оголошуємо рядки
let line1 = "Нехай завжди буде сонце, ";
let line2 = "Нехай завжди буде небо, ";
let line3 = "Нехай завжди буде мама, ";
let line4 = "Нехай завжди буду я.";

// Об'єднуємо рядки за допомогою методу concat()
let poem = line1.concat(line2, line3, line4);

console.log(poem);